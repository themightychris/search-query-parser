{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch parser",
            "type": "php",
            "request": "launch",
            "program": "${workspaceRoot}/QueryParser.php",
            "cwd": "${workspaceRoot}",
            "port": 9000
        }
    ]
}